[package]
name = "longbridge-bridge"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
longbridge = "4.1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1.0", features = ["io-std", "io-util", "macros", "rt-multi-thread", "sync"] }
